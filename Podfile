platform :ios, '9.0'

target 'The MovieDB (Test)' do
 	# Comment the next line if you're not using Swift and don't want to use dynamic  	frameworks
  	use_frameworks!
  	# Pods for The MovieDB (Test)
    	pod 'Alamofire'
        pod 'SwiftyJSON', '~> 4.0'
        pod 'Alamofire-SwiftyJSON'
end
